# ADRs — Index

Architecture Decision Records for the WaveFit frontend.

No ADRs are written yet. Record every decision that shapes the architecture as
`documents/decisions/adr-<nnn>-<short-title>.md` (one per decision, dated,
immutable — add a new ADR instead of rewriting an old one).

Rationale currently lives implicitly in [engineering docs](../engineering/README.md)
and [Specs](../../sdd/README.md).
